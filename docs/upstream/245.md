# Upstream #245 — bitrate characterization

Issue: https://github.com/nulloy/nulloy/issues/245
Base: `027d81a583b07457a4fa5f18b3e7dcca50b05b58`.

## Assessment

Read the live issue and all three comments. The reporter no longer remembers the
format, and the maintainer still needs the sample. No issue-specific or keyword
PR was found (`#245`, `bitrate`, `audio properties`). The fork's existing PR #6
resource fixes were reviewed; none replaces the `%B` calculation.

No missing-bitrate defect reproduced. Do not claim this issue fixed. The real
`NTagReaderTaglib` returns nonempty bitrate for generated CBR/VBR MP3, PCM WAV,
FLAC, Vorbis, Opus and WavPack. Missing and unsupported files yield empty values,
as does clearing the source. No production change is justified by these results.

## Runnable coverage

```sh
cmake -S tests/upstream/245 -B build-issue245 -G Ninja
cmake --build build-issue245 -j1
ctest --test-dir build-issue245 --output-on-failure
```

The standalone Qt Test harness compiles the real TagLib reader and creates its
own three-second sine fixtures with ffmpeg. No downloaded music is bundled.
Requires Qt Core/Gui/Test (Core5Compat with Qt 6), TagLib and ffmpeg with the named
encoders. Qt 5/6 CMake support is retained; local execution used Qt 6.8.2,
TagLib 2.0.2 and Linux, not the Windows release package.

Eight behavior rows plus setup/cleanup pass. Measured kbps were MP3 CBR 129,
MP3 VBR 34, WAV 1411, FLAC 103, Vorbis 24, Opus 118, WavPack 334. The first test
incorrectly required exactly 128 for CBR; TagLib reports a rounded file average
including MP3 header overhead. The final assertion permits 127–130 rather than
changing production code to force the encoder's nominal number.

Limits: this checks the metadata backend, not visible skin rendering, unknown
tracker bitrate, or the missing original file. Next test: obtain the reporter's
file and format string; compare TagLib audio properties, reader `B`, and actual
`%B` rendering on the Windows package before selecting a fix.
