cmake_minimum_required(VERSION 3.16)
project(NulloyIssue241 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_AUTOMOC ON)
find_package(QT NAMES Qt6 Qt5 REQUIRED COMPONENTS Core Gui Test)
find_package(Qt${QT_VERSION_MAJOR} REQUIRED COMPONENTS Core Gui Test)
if(QT_VERSION_MAJOR EQUAL 6)
    find_package(Qt6 REQUIRED COMPONENTS Core5Compat)
endif()
find_package(PkgConfig REQUIRED)
pkg_check_modules(TAGLIB REQUIRED IMPORTED_TARGET taglib)
set(ROOT "${CMAKE_CURRENT_LIST_DIR}/../../..")
add_executable(testIssue241 testIssue241.cpp
    ${ROOT}/src/plugins/pluginTaglib/tagReaderTaglib.cpp)
target_include_directories(testIssue241 PRIVATE ${ROOT}/src ${ROOT}/src/interfaces
    ${ROOT}/src/plugins/pluginTaglib)
target_link_libraries(testIssue241 PRIVATE Qt${QT_VERSION_MAJOR}::Core
    Qt${QT_VERSION_MAJOR}::Gui Qt${QT_VERSION_MAJOR}::Test PkgConfig::TAGLIB)
if(QT_VERSION_MAJOR EQUAL 6)
    target_link_libraries(testIssue241 PRIVATE Qt6::Core5Compat)
endif()
enable_testing()
add_test(NAME issue241 COMMAND testIssue241)
